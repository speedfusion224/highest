{
  "name": "highest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.js && rm -rf dist && mkdir -p dist && cp -r src/* dist/",
    "preview": "npm run build && cd dist && python3 -m http.server 8080",
    "sync:ios": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "5.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "5.0.0",
    "@capacitor/ios": "5.0.0"
  },
  "author": "highest",
  "license": "MIT"
}
