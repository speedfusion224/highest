{
  "name": "highest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.js && rm -rf dist && mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {},
  "author": "highest",
  "license": "MIT"
}
